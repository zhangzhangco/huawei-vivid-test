# HDR色调映射专利可视化工具依赖

# 核心框架
gradio>=4.0.0
numpy>=1.21.0
scipy>=1.7.0

# 图像处理
opencv-python>=4.5.0
Pillow>=8.0.0
imageio>=2.9.0

# 可视化
matplotlib>=3.5.0
plotly>=5.0.0

# 数据处理
pandas>=1.3.0

# 配置和日志
pyyaml>=6.0
loguru>=0.6.0

# 性能监控
psutil>=5.8.0

# GPU加速支持 (推荐)
numba>=0.56.0      # Numba JIT编译加速
cupy-cuda12x>=13.0.0  # CUDA GPU加速 (CUDA 12.x)

# 开发和测试
pytest>=6.0.0
pytest-cov>=3.0.0
black>=22.0.0
flake8>=4.0.0

# OpenEXR支持 (可选)
# OpenEXR>=1.3.0  # 取消注释以启用EXR支持